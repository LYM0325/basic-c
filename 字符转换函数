#include<stdio.h>
//tolower toupper 
int  main()
{
	char arr[20] = {0};
	scanf("%s",arr);
	int i = 0;
	while(arr[i])
	{
		if(isupper(arr[i]))
		{
			arr[i] = tolower(arr[i]); 
		}
		i++;
	}
	puts(arr);
	return 0;
}
